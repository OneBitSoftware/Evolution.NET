using ListOfNumbers = System.Collections.Generic.List<int>;

// These are new features in C# 12
// using Money = decimal;
// using Stats = (int Sum, int Min, int Max);
// using Stats = (int, int, int);

Console.WriteLine("Hello, world!");
